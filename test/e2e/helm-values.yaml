# Helm values for E2E testing
replicaCount: 1

image:
  repository: valkey-operator
  pullPolicy: Never
  tag: test

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 10m
    memory: 64Mi

leaderElection:
  enabled: false
